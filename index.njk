---
layout: layouts/base.njk
title: Home
---

<section class="hero">
  <h2>Real-Time Cybersecurity Threats & Tools</h2>
  <p>Actionable insights, breaking threats, and hands-on toolkits to keep you ahead of attackers.</p>
</section>

<section class="content-grid">
  {% set tagLabels = {
    "tools": "🛠 Tool of the Week",
    "threats": "⚠️ Live Threat Alert",
    "vulnerabilities": "📊 Vulnerability Breakdown",
    "explainers": "🎓 Explainer",
    "redteam": "📚 Red Team Diaries",
    "research": "💡 Latest Research"
  } %}

  {% for tag, label in tagLabels %}
    {% set found = false %}
    {% for post in collections.post | reverse %}
      {% if not found and (post.data.tags | hasTag(tag)) %}
        <div class="card">
          <a class="a-no-style" href="{{ post.url }}">
            <h3>{{ label }}</h3>
            <p>{{ post.data.excerpt }}</p>
          </a>
        </div>
        {% set found = true %}
      {% endif %}
    {% endfor %}
  {% endfor %}

</section>

<section class="latest-posts">
  <h3>🆕 Latest Insights</h3>
  <div class="post-list">
    {% for post in collections.post | reverse | limit(5) %}
      <a href="{{ post.url }}" class="post-preview a-no-style">
        {% if post.data.cover %}
          <img src="{{ post.data.cover }}" alt="{{ post.data.title }}" class="cover-img" />
        {% endif %}
        <div class="post-box">
          <h4 class="post-preview-title">{{ post.data.title }}</h4>
          <p>{{ post.data.excerpt }}</p>
          <small>{{ post.date | readableDate }}</small>
        </div>
      </a>
    {% endfor %}
  </div>
</section>

<section class="newsletter-cta">
  <h3>Stay Ahead of the Curve</h3>
  <p>Get the latest threat intel, tools, and tactics — straight to your inbox.</p>
  <form>
    <input type="email" placeholder="Your email" />
    <button type="submit">Subscribe</button>
  </form>
</section>

